# Changelog

All notable releases of Vynox are listed here. Every entry corresponds to a Git tag and a
GitHub Release that carries the signed APK; the download page always links to the newest one.

## Unreleased

Development happens on feature branches and lands on `main` through pull requests once CI
(build + tests + release verification) is green.

